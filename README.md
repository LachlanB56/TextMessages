#Threaded text terminal

To gain pratice with basic software concepts I motified this message terminal to use a thread that concurrently reads from the file 
every two seconds and writes the user whenever a new message arises. 
